# GR1 Project - Dubbing (Thuyết minh phim tự động) - Backend
# Instructions
- Copy content of file ``.env-example`` to ``.env`` and change `DOCKER_FILE` variable to `Dockerfile`.
- Run `sudo docker-compose up` to start server
